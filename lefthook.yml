commit-msg:
  commands:
    commitlint:
      run: pnpm exec commitlint --edit {1}

pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{ts,js}"
      run: pnpm lint
    typecheck:
      run: pnpm exec tsc --noEmit
