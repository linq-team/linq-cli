/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";
import { ChatHandle, ChatHandle$inboundSchema } from "./chat-handle.js";
import { SentMessage, SentMessage$inboundSchema } from "./sent-message.js";

export type CreateChatResultChat = {
  /**
   * Unique identifier for the created chat (UUID)
   */
  id: string;
  /**
   * Display name for the chat. Defaults to a comma-separated list of recipient handles. Can be updated for group chats.
   */
  displayName: string | null;
  /**
   * Messaging service used
   */
  service: string;
  /**
   * Whether this is a group chat
   */
  isGroup: boolean;
  /**
   * List of participants in the chat. Always contains at least two handles (your phone number and the other participant).
   */
  handles: Array<ChatHandle>;
  /**
   * A message that was sent (used in CreateChat and SendMessage responses)
   */
  message: SentMessage;
};

/**
 * Response for creating a new chat with an initial message
 */
export type CreateChatResult = {
  chat: CreateChatResultChat;
};

/** @internal */
export const CreateChatResultChat$inboundSchema: z.ZodMiniType<
  CreateChatResultChat,
  unknown
> = z.pipe(
  z.object({
    id: types.string(),
    display_name: types.nullable(types.string()),
    service: types.string(),
    is_group: types.boolean(),
    handles: z.array(ChatHandle$inboundSchema),
    message: SentMessage$inboundSchema,
  }),
  z.transform((v) => {
    return remap$(v, {
      "display_name": "displayName",
      "is_group": "isGroup",
    });
  }),
);

export function createChatResultChatFromJSON(
  jsonString: string,
): SafeParseResult<CreateChatResultChat, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CreateChatResultChat$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CreateChatResultChat' from JSON`,
  );
}

/** @internal */
export const CreateChatResult$inboundSchema: z.ZodMiniType<
  CreateChatResult,
  unknown
> = z.object({
  chat: z.lazy(() => CreateChatResultChat$inboundSchema),
});

export function createChatResultFromJSON(
  jsonString: string,
): SafeParseResult<CreateChatResult, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CreateChatResult$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CreateChatResult' from JSON`,
  );
}
