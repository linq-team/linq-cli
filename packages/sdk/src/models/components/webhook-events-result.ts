/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";
import {
  WebhookEventType,
  WebhookEventType$inboundSchema,
} from "./webhook-event-type.js";

export type WebhookEventsResult = {
  /**
   * List of all available webhook event types
   */
  events: Array<WebhookEventType>;
  /**
   * URL to the webhook events documentation
   */
  docUrl: "https://apidocs.linqapp.com/documentation/webhook-events";
};

/** @internal */
export const WebhookEventsResult$inboundSchema: z.ZodMiniType<
  WebhookEventsResult,
  unknown
> = z.pipe(
  z.object({
    events: z.array(WebhookEventType$inboundSchema),
    doc_url: types.literal(
      "https://apidocs.linqapp.com/documentation/webhook-events",
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "doc_url": "docUrl",
    });
  }),
);

export function webhookEventsResultFromJSON(
  jsonString: string,
): SafeParseResult<WebhookEventsResult, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => WebhookEventsResult$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'WebhookEventsResult' from JSON`,
  );
}
